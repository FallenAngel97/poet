{
  "private": true,
  "workspaces": [
    "poet-examples",
    "@decodeapps/neopoet"
  ],
  "devDependencies": {
    "chai": ">= 1.1.1",
    "express": ">= 4.5.0",
    "mocha": "^9.0.2"
  },
  "scripts": {
    "test": "yarn --cwd poet test",
    "start": "cd ./examples && node ./defaultSetup.js",
    "test:watch": "yarn --cwd @decodeapps/neopoet test:watch"
  }
}
