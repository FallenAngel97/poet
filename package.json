{
  "private": true,
  "workspaces": ["poet-examples", "poet"],
  "dependencies": {
    "front-matter": ">= 0.1.x",
    "fs-then": "0.1.x",
    "json-front-matter": ">= 0.1.4",
    "jstransformer": "^1.0.0",
    "marked": ">=0.6.2",
    "pug": "^3.0.2",
    "when": "2.1.x"
  },
  "devDependencies": {
    "chai": ">= 1.1.1",
    "express": ">= 4.5.0",
    "mocha": "^9.0.2",
    "supertest": ">= 0.0.1"
  },
  "scripts": {
    "test": "yarn --cwd poet test",
    "test:watch": "yarn --cwd poet test:watch"
  }
}
